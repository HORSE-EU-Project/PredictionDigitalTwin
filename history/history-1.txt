# INSTALL INSTRUCTIONS, to be transformed in a single script

# Connecting to HORSE Github
git clone git@github.com:HORSE-EU-Project/PredictionDigitalTwin.git
ssh-keygen -t ed25519 -C "fabrizio.granelli@unitn.it"
eval "$(ssh-agent -s)"
cat ~/.ssh/id_ed25519.pub
# Install key to Github webpage in Settings/SSH Keys
# Test connectivity to Github repository
ssh -T git@github.com

# Download HORSE NDT
cd
git clone git@github.com:HORSE-EU-Project/PredictionDigitalTwin.git
mv PredictionDigitalTwin/ comnetsemu/

# Routine updates
sudo apt update
sudo apt upgrade

# Installing comnetsemu packages
sudo apt install git iperf iperf3 make pkg-config python3 python3-dev python3-pip sudo ansible

# Installing comnetsemu software
cd comnetsemu/util
bash ./install.sh -a
sudo usermod -aG docker $USER
newgrp docker

# Installing HORSE NDT requirements
cd 

# Installing Java
cd comnetsemu/app/comnetsemu_5G_customization
./install_java.sh

# Installing UERANSIM and Open5GS from Docker Hub
cd ../build
./dockerhub_pull.sh

# Installing updated containers (UE)
cd ../customization
./build.sh

# Input the correct IP in the config.ini file in /scripts directory
# Maybe rebuild the 5G containers?
